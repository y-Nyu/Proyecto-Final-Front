const initialState = {
    users: [],
    usersCopy: [],
    items: [],
    itemsCopy: []
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return {...state}
    }
}

export default reducer;